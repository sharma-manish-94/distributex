services:
  ingestion-api:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.ingestion
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    networks:
      - distributex-net

networks:
  distributex-net:
    driver: bridge